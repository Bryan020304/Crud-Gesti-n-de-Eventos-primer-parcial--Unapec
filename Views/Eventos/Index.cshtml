@model IEnumerable<EventosCRUD.Models.Evento>

@{
    ViewData["Title"] = "Eventos";
}

<h1><i class="bi bi-calendar-event me-2"></i>Lista de Eventos</h1>

<p>
    <a asp-action="Create" class="btn btn-primary"><i class="bi bi-plus-lg me-1"></i>Crear Nuevo Evento</a>
    <a asp-action="Dashboard" class="btn btn-success"><i class="bi bi-bar-chart me-1"></i>Ver Dashboard</a>
</p>

@if (TempData["Success"] != null)
{
    <div class="alert alert-success">
        @TempData["Success"]
    </div>
}

<table class="table table-striped">
    <thead>
        <tr>
            <th>Evento</th>
            <th>Fecha</th>
            <th>Lugar</th>
            <th>Asistentes / Capacidad</th>
            <th>Disponibilidad</th>
            <th>Estado</th>
            <th>Acciones</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    <strong>@Html.DisplayFor(modelItem => item.Nombre)</strong><br>
                    <small class="text-muted">@item.Descripcion</small>
                </td>
                <td>@Html.DisplayFor(modelItem => item.Fecha)</td>
                <td>@Html.DisplayFor(modelItem => item.Lugar)</td>
                <td>
                    @item.AsistentesRegistrados / @item.CapacidadMaxima
                    <br>
                    <small>@item.PorcentajeOcupacion</small>
                </td>
                <td>
                    @if (item.Disponibilidad == "Agotado")
                    {
                        <span class="badge bg-danger">@item.Disponibilidad</span>
                    }
                    else if (item.Disponibilidad == "Últimos lugares")
                    {
                        <span class="badge bg-warning">@item.Disponibilidad</span>
                    }
                    else
                    {
                        <span class="badge bg-success">@item.Disponibilidad</span>
                    }
                </td>
                <td>
                    @if (item.Estado == "Hoy")
                    {
                        <span class="badge bg-danger">@item.Estado</span>
                    }
                    else if (item.Estado == "Próximo")
                    {
                        <span class="badge bg-warning">@item.Estado</span>
                    }
                    else if (item.Estado == "Finalizado")
                    {
                        <span class="badge bg-secondary">@item.Estado</span>
                    }
                    else
                    {
                        <span class="badge bg-info">@item.Estado</span>
                    }
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-sm btn-warning"><i class="bi bi-pencil me-1"></i>Editar</a>
                    <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-sm btn-info"><i class="bi bi-eye me-1"></i>Detalles</a>
                    <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-sm btn-danger"><i class="bi bi-trash me-1"></i>Eliminar</a>
                </td>
            </tr>
        }
    </tbody>
</table>

@if (!Model.Any())
{
    <div class="alert alert-info">
        No hay eventos registrados. ¡Crea tu primer evento!
    </div>
}